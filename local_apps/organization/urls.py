from django.conf.urls import url
from django.contrib import admin

from local_apps.organization import views as organization_views

urlpatterns = [
    # url(r'^$', organization_views.home, name="Home"),
]
